# EdgeLiteGateway Web 前端环境变量示例
# 复制此文件为 .env 并修改实际值
# Vite 环境变量必须以 VITE_ 前缀开头

# ============================================
# API 基础地址（生产环境需指向实际后端地址）
# ============================================
# 方式1：前端独立部署，直接请求后端（需配置 CORS）
# VITE_API_BASE_URL=http://localhost:8080/api/v1
# VITE_WS_BASE_URL=ws://localhost:8080

# 方式2：通过 Nginx 反向代理（推荐生产环境使用）
# 此时前端和后端在同域下，使用相对路径即可
VITE_API_BASE_URL=/api/v1
# WS_BASE_URL 只需设置协议+主机，路径由代码自动拼接
# 留空则自动使用当前页面的协议和主机
VITE_WS_BASE_URL=
